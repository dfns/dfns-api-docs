{
  "name": "@dfns/universe-input-validators",
  "version": "2.13.11",
  "license": "SEE LICENSE IN DFNS_LICENSE",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "16.9.1",
    "rimraf": "3.0.2",
    "typescript": "4.2.3"
  },
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "node_modules/.bin/tsc",
    "cb": "npm install && npm run clean && npm run build",
    "deploy": "npm install && npm run cb && npm publish --access=private"
  },
  "main": "dist/index.ts"
}
